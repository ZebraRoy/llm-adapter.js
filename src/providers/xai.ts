import type { XAIConfig } from '../types/index.js';
import type { LLMAdapter } from '../core/adapter.js';

/**
 * xAI adapter - OpenAI-compatible
 * TODO: Move full implementation from original index.ts
 * @param config - xAI configuration
 * @returns LLM adapter for xAI
 */
export function createXAIAdapter(config: XAIConfig): LLMAdapter {
  // Temporary stub - full implementation would be moved from original file
  throw new Error("xAI adapter not yet fully implemented in refactored structure");
} 