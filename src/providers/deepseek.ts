import type { DeepSeekConfig } from '../types/index.js';
import type { LLMAdapter } from '../core/adapter.js';

/**
 * DeepSeek adapter - OpenAI-compatible
 * TODO: Move full implementation from original index.ts
 * @param config - DeepSeek configuration
 * @returns LLM adapter for DeepSeek
 */
export function createDeepSeekAdapter(config: DeepSeekConfig): LLMAdapter {
  // Temporary stub - full implementation would be moved from original file
  throw new Error("DeepSeek adapter not yet fully implemented in refactored structure");
} 